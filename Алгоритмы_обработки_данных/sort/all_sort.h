#include <malloc.h>;
#include <string.h>;

typedef int BaseType;

void qs(BaseType*, BaseType, BaseType);
void swap(BaseType *, BaseType *);

void ShellsSort(BaseType *, unsigned);

void radixsort(BaseType *, BaseType);